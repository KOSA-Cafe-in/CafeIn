<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<annotation-driven/>
	<resources mapping="/resources/**" location="/resources/" />

	<!-- admin/user 각각의 home.jsp로 바로 매핑 -->
	<view-controller path="/user/home"  view-name="user/home"/>
	<view-controller path="/admin/home" view-name="admin/home"/>
	
	<!-- 기존 /home 또는 / 로 들어오면 user 홈으로 리다이렉트 -->
	<view-controller path="/home" view-name="redirect:/user/home"/>
	<view-controller path="/"     view-name="redirect:/user/home"/>

	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.cafein"></context:component-scan>
	<!-- AWS S3 연동용 프로퍼티 -->
	<context:property-placeholder location="classpath:s3.properties" />

	<!-- 파일 업로드 지원 -->
	<beans:bean id="multipartResolver"
        class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />

</beans:beans>
